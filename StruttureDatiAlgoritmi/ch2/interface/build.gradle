group 'interface'
version '1.0-SNAPSHOT'

apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'dm.ex.java.StringProcessor'

sourceCompatibility = 1.7
targetCompatibility = 1.7

jar{
    baseName = 'interface'
    version = '0.0.1'
    manifest{
        attributes('Implementation-Title':'Interface',
        'Implementation-Version':version)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}

task Wrapper(type: Wrapper){
    gradleVersion = '2.3'
}
