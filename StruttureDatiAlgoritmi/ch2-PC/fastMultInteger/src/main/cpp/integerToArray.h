#ifndef INTEGERTOARRAY_H
#define INTEGERTOARRAY_H

extern const int MAX;
int* integerToArray(int);
int* sum(const int*, const int*,int = 0);
// True if you want to free the allocated memory. Default to false
void displayInteger(const int *, int n, bool = false);

#endif
